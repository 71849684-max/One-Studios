# Código nuevo

El código nuevo de One Studios se organiza por capas y dominios. El código productivo legacy permanece temporalmente en `index.html` y `assets/` mientras se extrae con pruebas de regresión.

No agregar nuevos módulos versionados o hotfixes directamente bajo `assets/js/`.
